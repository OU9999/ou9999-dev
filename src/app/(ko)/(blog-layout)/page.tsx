import { HomePageContent } from "@/components/main-section/home-page-content";
import { defaultLocale } from "@/i18n/config";

const HomePage = () => {
  return <HomePageContent locale={defaultLocale} />;
};

export default HomePage;
